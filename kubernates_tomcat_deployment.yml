apiVersion: apps/v1
kind: Deployment1
metadata:
  name: tomcat2
spec:
  selector:
    matchLabels:
      app: tomcat
  replicas: 2
  template:
    metadata:
      name: tomcat
      labels:
        app: tomcat
    spec:
      containers:
      - image: rajiv4671/masterapp  
        name: tomcatapp
